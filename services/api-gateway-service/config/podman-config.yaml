server:
  address: 0.0.0.0
  port: 5000
  read_timeout: 30s
  write_timeout: 30s
  idle_timeout: 120s

grpc_services:
  auth:
    address: "auth-service:50051"
    timeout: 30s

logging:
  level: "info"
  format: "json"
